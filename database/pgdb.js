const humps = require('humps') // used to camel case the column names from the returned table

module.exports = pgPool => {
    // insert your postgreSQL queries here ending with calling humps.camelizeKeys on the result
}