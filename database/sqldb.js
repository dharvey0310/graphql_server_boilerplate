const humps = require('humps') // used to camel case the column names from the returned table
const sql = require('mssql')

module.exports = () => {
    // enter you SQL Server (sql.query`query text`) here calling humps.camelizeKeys on the result
}